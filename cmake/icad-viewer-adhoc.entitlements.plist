<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Local ad-hoc builds have no common Team ID. Permit only the bundled,
         app-sealed Qt frameworks to load; the outer bundle remains signed. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Qt's QRegularExpression uses PCRE2's guarded JIT for live syntax
         highlighting. Hardened runtime must explicitly permit that mapping. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
</dict>
</plist>
