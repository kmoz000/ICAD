# Native JSON

This module provides the dependency-free JSON value, strict parser, and
deterministic serializer used at protocol boundaries. It rejects duplicate
object keys, malformed numbers, control characters, invalid escapes, unpaired
UTF-16 surrogates, and trailing data.
