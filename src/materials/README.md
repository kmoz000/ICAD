# Predefined materials

This module owns stable preset names and canonical PBR/texture properties.
Semantic analysis resolves user material declarations here; renderers and
exporters consume lowered values rather than maintaining separate catalogs.
